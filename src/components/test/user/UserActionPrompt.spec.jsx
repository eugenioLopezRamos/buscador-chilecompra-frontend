import React from 'react';


const UserActionPrompt = (props) => {

    const component = props.component || null;
    // const actions = props.actions || null;
    // const menu = props.menu || null;

    return component;


}

export default UserActionPrompt;